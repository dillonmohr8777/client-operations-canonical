# Build outputs are regenerable in one command; the proof-voice audio is not a
# deliverable. Text evidence from a verified run is kept in samples/evidence/.
out/
samples/build/
samples/build-*/
node_modules/
*.log

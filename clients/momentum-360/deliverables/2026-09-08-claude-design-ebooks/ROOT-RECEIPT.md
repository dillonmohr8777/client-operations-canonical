# Root native verification, 2026-09-08

Authenticated Codex in-app browser, existing private Claude Design artifact.

Confirmed final lightweight Ebook 1 Play renders, Sources toggles, Motion pauses,
and the method diagram changes its selection and answer. Root applied the final
navigation fix directly to build_ebook.py: lift section containment, force layout,
scroll instantly, retain visible sections. Rebuilt all five artboards; Claude
seeded and saved this exact rebuild to the existing private artifact.

Final native test: Contents chapter 5 lands with its heading visible below the
sticky header and without preview navigation. What to do Monday navigates within
the same preview. First worksheet item changes from unchecked to checked and the
progress text changes to 1 / 13. Previous cold-layout limitation is resolved by
this root confirmation. These are representative native checks of the shared
implementation, not a claim that every control in every book was clicked.

All five local PDF companions have text and no nearly blank pages: 37, 32, 36,
33, 36 pages (174 total). Ordered source-token coverage in standalone HTML is
99.768% to 99.895%; formatting and added review material affect tokenization.
The full source manuscripts and publication review gates remain in the editions.

Known limitation: book reading font uses the fallback documented by Claude after
the native load reduction; the shared brand kit retains its self-hosted font assets.
No public publication approval is implied. All deliverables are private review drafts.

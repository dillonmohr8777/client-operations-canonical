const motion=document.getElementById('motion-toggle');motion.addEventListener('click',()=>{const paused=document.body.classList.toggle('motion-paused');motion.setAttribute('aria-pressed',String(paused));motion.textContent=paused?'Resume motion':'Pause motion'});
const hero=document.getElementById('brand-hero');if(hero)new IntersectionObserver(entries=>document.body.classList.toggle('hero-gone',!entries[0].isIntersecting),{threshold:0}).observe(hero);
const librarySearch=document.getElementById('library-search');if(librarySearch)librarySearch.addEventListener('input',()=>{const query=librarySearch.value.toLowerCase().trim();let count=0;document.querySelectorAll('.book-card').forEach(card=>{const match=card.dataset.search.includes(query);card.hidden=!match;if(match)count++});document.getElementById('library-count').textContent=`${count} field guide${count===1?'':'s'}`;document.getElementById('library-empty').hidden=count!==0});
const raw=document.getElementById('book-data');if(raw){const book=JSON.parse(raw.textContent);const nav=document.getElementById('reader-nav');document.getElementById('mobile-menu').addEventListener('click',e=>{const open=nav.classList.toggle('open');e.currentTarget.setAttribute('aria-expanded',String(open))});const text=document.getElementById('book-search'),results=document.getElementById('search-results');text.addEventListener('input',()=>{const q=text.value.trim().toLowerCase();results.replaceChildren();results.classList.toggle('show',q.length>1);if(q.length<2)return;const matches=book.chapters.filter(c=>(c.title+' '+c.text).toLowerCase().includes(q));for(const c of matches.slice(0,12)){const a=document.createElement('a');a.href='#'+c.id;const strong=document.createElement('strong');strong.textContent=c.title;a.append(strong);const pos=c.text.toLowerCase().indexOf(q);a.append(document.createTextNode(c.text.slice(Math.max(0,pos-40),Math.max(0,pos-40)+150)+'…'));results.append(a)}if(!matches.length)results.textContent='No matching passage. Try another word.'});
const observer=new IntersectionObserver(entries=>{for(const entry of entries)if(entry.isIntersecting){document.querySelectorAll('.chapter-links a').forEach(a=>{const active=a.hash==='#'+entry.target.id;a.classList.toggle('active',active);if(active)a.setAttribute('aria-current','location');else a.removeAttribute('aria-current')})}},{rootMargin:'-100px 0px -65% 0px'});document.querySelectorAll('.chapter').forEach(c=>observer.observe(c));const progress=document.getElementById('reading-progress');const update=()=>{const r=document.querySelector('.reader').getBoundingClientRect();progress.value=Math.max(0,Math.min(100,(-r.top+100)/(r.height-innerHeight+100)*100))};addEventListener('scroll',update,{passive:true});update();
const steps=['Identify the actual question, repeated task or missing information. Record the inputs and the person who owns the result.','Draw the handoff before choosing a tool. Specify what goes in, what should come out, and when a human must decide.','Try a bounded example. Compare the output with the expected result and record failures before expanding its use.','Ask the accountable owner to inspect the result. Keep the evidence and the next decision together.'];const detail=document.getElementById('diagram-detail');detail.textContent=steps[0];document.querySelectorAll('[data-step]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-step]').forEach(x=>x.setAttribute('aria-pressed',String(x===b)));detail.textContent=steps[Number(b.dataset.step)]}));
const key='momentum-v3-checklist:'+book.slug;let saved={};try{saved=JSON.parse(localStorage.getItem(key)||'{}')}catch{}document.querySelectorAll('[data-check]').forEach(c=>{c.checked=!!saved[c.dataset.check];c.addEventListener('change',()=>{saved[c.dataset.check]=c.checked;try{localStorage.setItem(key,JSON.stringify(saved));document.getElementById('check-status').textContent=`${Object.values(saved).filter(Boolean).length} of 4 complete. Saved only in this browser.`}catch{document.getElementById('check-status').textContent='Browser storage is unavailable. Your checks will last for this page visit only.'}})});}

const titleObserver=new IntersectionObserver(entries=>{for(const entry of entries)if(entry.isIntersecting){entry.target.classList.add("type-arrive");titleObserver.unobserve(entry.target)}},{threshold:.2});document.querySelectorAll(".cover-copy h1,.library-intro h1").forEach(e=>titleObserver.observe(e));
document.querySelectorAll('.citation-ref a').forEach(a=>a.addEventListener('click',()=>{const source=document.querySelector(a.hash);if(source)source.open=true}));
const gallery=document.getElementById('brand-gallery');if(gallery){const sets={agents:[['hero-mascot','Momo','The collection guide with the exact Momentum m superhero emblem.'],['search-mascot','Search & discovery','Momo studies a local business landscape.'],['phone-mascot','Lead response','A clear role for the next conversation.'],['build-mascot','Build & create','A practical companion for making the system.'],['operations-mascot','Operations','A coordinated view of the moving parts.'],['audience-mascot','Audience','People stay at the center of the work.']],services:[['icon-search','Search','Ceramic service icon.'],['icon-phone','Response','Ceramic service icon.'],['icon-build','Build','Ceramic service icon.'],['icon-operations','Operations','Ceramic service icon.'],['icon-audience','Audience','Ceramic service icon.'],['icon-proof','Proof','Ceramic service icon.']],imagery:[['bird-engraving','Observation','Original engraved bird study for editorial intervals.'],['botanical-engraving','Growth & structure','Original botanical study for the collection.'],['philly-engraving','Philadelphia','Original city engraving for place and context.']]};function showGallery(name){gallery.dataset.category=name;gallery.innerHTML=sets[name].map(([file,title,copy])=>`<figure><img src="assets/${file}.png" alt="${title}" loading="lazy"><figcaption><h3>${title}</h3><p>${copy}</p><a href="assets/${file}.png" download>Download original PNG</a></figcaption></figure>`).join('');document.querySelectorAll('[data-gallery]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.gallery===name)))}document.querySelectorAll('[data-gallery]').forEach(b=>b.addEventListener('click',()=>showGallery(b.dataset.gallery)));showGallery('agents');document.getElementById('identity-replay').addEventListener('click',e=>{const compact=document.getElementById('identity-demo').classList.toggle('compact');e.currentTarget.setAttribute('aria-pressed',String(compact));e.currentTarget.textContent=compact?'Return to full identity':'Preview header transition'})}

# Impeccable release exception — August 13 workshop update

The manual detector returned exit code 2 because this established surface predates the current portable design-token schema and contains existing visual-system findings across `index.html` and `styles.css`.

This release changes only the verified event date, WordPress embed support, and calendar-access copy/behavior. It does not introduce a new font, color, radius, type size, card treatment, grid, or decorative accent. The detector findings for Space Grotesk, the command-frame accent, the existing grid field, and undocumented legacy token literals therefore remain outside this narrow release scope.

The factual calendar correction is required for release: no August 13 organizer event currently exists, so the page no longer submits organizer-invitation consent or promises that an RSVP invitation will be sent. Registrants retain explicit Google, Outlook, and ICS save options.

`DESIGN.md` and `.impeccable/design.json` remain intentionally unchanged. Their reported drift should be handled through a separately approved Impeccable document refresh rather than as a side effect of this production date/embed update.

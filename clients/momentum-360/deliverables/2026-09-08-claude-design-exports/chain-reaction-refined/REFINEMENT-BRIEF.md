# Chain Reaction refinement

Create `AI 01 - Chain Reaction Refined.dc.html` as a separate derivative in the existing Launch project and conversation. Base it on the verified newest `AI 01 - Atomic Assembly.dc.html` and repaired runtime. Preserve every existing source file, master, and prior artifact. If runtime changes are needed, copy to a derivative-specific module instead of changing shared behavior.

Dillon specifically likes the piece ending “One idea. A chain reaction.” He wants more prominent opening typography and the most professional 3D treatment this existing scene can support.

Keep the 18-second chain-reaction narrative and deterministic 540-frame / 30fps contract, with separate authored 16:9 and 9:16 compositions. Keep exact Momentum logo assets, the approved midnight/navy/blue/gold palette, Bricolage Grotesque display exception and Nunito reading type, alpha object layer and native sound controls.

Opening: large, deliberate `CHAIN REACTION` typography must be fully legible within the first two seconds, including actual captures at 0.5, 1.2 and 2 seconds. Hold it long enough to read. Give the atom its own space and clear silhouette. In portrait use a deliberate stacked hierarchy and vertical arrangement, not a center crop. No late title after a long blank or particle-only opening. Paint all film typography into the exportable native layer, not only DOM UI.

Story: atom and title establish the idea, a readable impulse initiates the branching reaction, the branches gather into three coordinated structures, the structures form one system, and the exact logo and “One idea. A chain reaction.” close the film. Preserve the causal progression; no generic particle wallpaper or unrelated stock scene.

Craft: refine the core geometry and surface response, use a deliberate key/fill/rim balance, keep materials distinct and highlights controlled, give the orbitals and particles readable depth, and use restrained, smoothly eased camera arcs with settled compositions. Refine transitions without excessive bloom, shaking, rapid zooms, or arbitrary spins. Use clean negative space and clear subject dominance. Do not invent metrics or product outcomes.

Expose `window.__momentumFilm.ai01refined` with `renderFrameAt(n)`, `capturePNG()`, `setAspect`, authoredSeconds=18, fps=30 and frames=540. Preserve keyboard/scrub/replay/pause/aspect/sound and reduced-motion behavior. Confirm the approved font actually paints before texture generation. Verify the opening type and complete end card at full 1920×1080 and 1080×1920 with safe margins. Test frame300→120→300 byte identity and inspect native exported PNG probes.

Implement the actual artifact and write a truthful native build receipt. No new project or chat, no external send, publishing, deployment, spending, subscription, or paid render. Local MP4 production will follow the downloaded source, and will be labeled silent review unless audio is separately mastered.

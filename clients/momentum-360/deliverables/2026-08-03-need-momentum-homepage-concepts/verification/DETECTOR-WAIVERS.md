# Impeccable detector disposition

The required manual detector was run after the Jenny feedback implementation on August 13, 2026. It exited with advisory findings across the project's monolithic shared stylesheet. This is a narrow completion exception, not a disabled check.

The stylesheet contains the untouched Map concept, the existing Signal visual system, and the newly revised Signal sections in one file. The detector therefore reported legacy literal-value advisories and two documented intentional choices outside this change: Instrument Serif remains quarantined to the Map phrase, and the Map surface retains its bounded grid treatment. The portable `.impeccable/design.json` metadata also predates the current `DESIGN.md`; repairing that known documentation drift was intentionally kept out of this scoped client-feedback pass.

The changed Signal surface was separately verified at desktop and mobile widths. The review confirmed the exact navigation order, no horizontal overflow, accessible tap-target sizing, reduced-motion rules, localized team imagery, localized social icons, and no browser console errors. TypeScript, content tests, and the production build pass. The only browser warning is Three.js's upstream `Clock` deprecation from the existing particle system.

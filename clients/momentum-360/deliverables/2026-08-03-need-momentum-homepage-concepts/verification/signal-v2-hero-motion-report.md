# Kinetic Signal Field v2 motion report

The five-frame contact sheet is `signal-v2-hero-motion-contact-sheet.png`.

## Sequence

- 0.8s: the particle field is fully dispersed and already in motion.
- 4.6s: the field begins condensing toward the supplied Momentum mark.
- 5.9s: the exact mark is resolved and surrounded by the remaining signal halo.
- 7.2s: the logo hold releases into a directional dispersal.
- 8.7s: the field has returned to its free signal state and is ready to loop.

## Quality gate

- Composition stays stable while the particle state changes.
- The headline, navigation, and CTAs do not move with the WebGL scene.
- The logo is sampled from the supplied transparent artwork and reinforced by the same exact PNG at the resolved state.
- Motion uses GPU point attributes and opacity/transform layers rather than layout animation.
- Reduced motion shows the exact static mark and collapses automatic UI animation.

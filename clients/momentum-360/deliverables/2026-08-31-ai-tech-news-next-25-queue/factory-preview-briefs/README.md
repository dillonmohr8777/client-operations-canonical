# Factory preview briefs

These JSON briefs were filled from official first-party pages and passed to the existing `dillon-os` `_templates/site-factory/build-site.js`. They are not a second generator.

Built output stayed in `/tmp/leftover-factory-preview/`. `noindex` is on. Do not deploy from this environment.

Facts-only: phones, addresses, and hours were copied from the official URLs recorded in each brief. Images in the local preview are labeled placeholders until `harvest.js` runs.

{
  "name": "puttery-tock-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Durable public webhook relay for the Puttery NYC Tock reservation integration. Tock posts, the local receiver drains.",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "deploy:preview": "netlify deploy",
    "deploy:prod": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.2"
  }
}

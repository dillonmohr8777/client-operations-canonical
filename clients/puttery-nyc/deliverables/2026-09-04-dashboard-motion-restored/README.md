# Puttery original motion restored

Restoration preview: http://127.0.0.1:65515/

Deploy only `public/`. The original source remains untouched at `C:\Users\dillo\Documents\Codex\2026-08-04\we-can-help-right-research-this-2`.

The full original stylesheet is preserved as an exact prefix, with all14original keyframes. The2.1secondintro, moving wordmark, animated SVGcourse icons, golf-ball progress, side ornament, large Fugaz headlines, black/pink/teal fields, original layout and artwork are restored. The intro seen key is versioned for this restoration and Replay intro repeats it. The existing69questionIDs, answer storagekey and export remain intact.

Current factual copy is carried forward: reservation webhook receiving; NYC filtering verified; reconciliation and exports pending; expanded access reviews requested. Neither webhook updates nor engineering checks are reported as bookings or attributed revenue.

Additive behavior is limited to explicit Pause/Resume, offscreen/hidden-page pause, original reduced-motion support, a small sidebar golf-ball fallback, and safe storage-error feedback. The sidebar footer flows after navigation rather than overlapping it on short viewports.

`qa/checks.json` records39/39passingchecks. `qa/motion-proof.json` records time-separated rendered transforms for the logo and a visible SVGicon, plus the later intro stamp. `qa/restoration-proof.json` records preservation against the original source. `qa/public-manifest.json` contains the public bundle hashes. Independent QA owns release sign-off.

The detector returns exit2with107advisory/warningfindings, zero critical findings. `qa/detector-exceptions.md` explains the narrow exception for restoring the exact user-selected original direction. This is not presented as a clean detector pass.

The restored `higgsfield-course-band.svg` is the original named asset; its filename alone does not verify that a generator created it. The final public bundle now includes the parent-verified six-second champion golfer film, its exact SVG reveal, and the separate generated golf loop with posters. The 17-file bundle is frozen; generation provenance and composed recording remain private under `qa/`. Read `DELIVERY.md` for the verified release and delivery receipt.

Champion behavior is documented in `DESIGN.md` and generated sidecar metadata. The final 20 focused checks and six confirmation checks pass, including full-golfer mobile framing, Skip, Pause, Escape, replay, reduced motion and both initial/late media failure recovery. The final detector includes `index.html`, `styles.css`, `app.js`, `champion-intro.js` and `media.js`; it returns exit 2 with 99 findings, zero critical, under the appended narrow exception.

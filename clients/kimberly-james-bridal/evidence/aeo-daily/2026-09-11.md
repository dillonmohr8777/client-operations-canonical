# KJB AEO daily — 2026-09-11 (dry-run)

**clientId:** kimberly-james-bridal  
**domain:** kimberlyjamesbridal.com  
**locator:** squarespace-production (presence only)  
**pin/approval:** none → no mutation

## Verified deltas
1. **CRITICAL** `chg-kjb-nap-localbusiness-8333` — Visible NAP **8333 Germantown Avenue, Philadelphia, PA 19118**; JSON-LD Organization+LocalBusiness (and map Business Info) still **15 West Highland Avenue**. Confirmed on `/`, `/scheduling`, `/faq`.
2. `chg-kjb-hours-string-cleanup` — LocalBusiness `openingHours` begins with a leading comma.
3. `chg-kjb-llms-txt` — `/llms.txt` 404 (defer until after NAP fix).
4. `chg-kjb-faqpage-money-pages` — FAQPage on `/faq` only; not on `/scheduling`.

## Acceptance (for apply later)
- Public JSON-LD streetAddress = 8333 Germantown Ave on `/` and `/scheduling`; Highland absent from schema/map Business Info.
- Valid hours; `/llms.txt` 200 with correct NAP if approved.

## Non-actions
No CMS edit, no Bitwarden unlock, no client send.

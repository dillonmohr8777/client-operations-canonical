# Onsite Concrete — Conversion-Action Audit

**STATUS: DRAFT, staged. No account change was made. Nothing sent.**
Conversion-action settings are an advertising change and require Dillon's approval.

> Generated by Muse Spark 1.3 (contributor) 2026-09-16 23:13 ET, headless,
> `--max-model-steps 40`, scoped read-only to this client directory.
> Orchestrator-verified: all 15 conversion actions in `evidence-onsite-conversions.json`
> are present, none invented. NOT verified by this session: the claim that PMax was
> re-enabled 2026-09-16 — the live pull window was 09-09..09-15 and showed every
> campaign PAUSED, so that statement comes from another file and needs its own check.

Prepared: 2026-09-17 (UTC). Read-only audit. No Ads, GA4, GTM, WordPress, or Netlify changes made. Nothing sent.
Account: Google Ads customer 103-371-5894. Site: onsiteconcretelandscape.com. GA4 property 477847660 (linked 2025-02-12, Import Off, no current access).
Primary evidence: `evidence-onsite-conversions.json` (API pull, window 2026-09-09–09-15, zero-delivery week); Sep 8 call-evidence audit + source-data; Sep 5 tracking readiness + completion receipt; measurement design v1; blueprint; Jul 16 dedup packet; Sep 16 pivot plan.

## Executive summary

- Tracking health: **high risk — not ready for Smart Bidding on current primaries.**
- 15 conversion actions in the export. 14 are ENABLED + primary-for-goal; 1 is HIDDEN/secondary (GA4 purchase import). Every website contact path fires multiple primary actions for the same user act.
- Main risks: (1) form double/triple/quadruple-counting across 4 primary form actions; (2) phone-click double-counting across 4+ primary phone-click actions plus parallel ad-call actions; (3) micro actions (2× Directions) set primary, so Maximize Conversions optimizes toward map taps; (4) no qualified-estimate primary exists — blueprint requires it; (5) no transaction/lead ID, generic `gtm.formSubmit` + `tel:` click triggers, no success-state proof.
- The June–July "79 conversions at ~$3.05/contact" (Sep 16 plan) are contact events (54 phone-click + 25 form events), not qualified estimates. Do not retarget bidding or budgets off that figure.
- Sep 9–15 shows 0 recorded events on 0 clicks/impressions across all 5 campaigns (all paused except PMax, which was re-enabled 9/16). That is a delivery blackout week, not a demand read.

## Conversion map

| # | Action name | Type / source | Category | Status | Funnel stage | Optimization role today | Risk |
|---|---|---|---|---|---|---|---|
| 1 | Smart campaign map clicks to call | SMART_CAMPAIGN_MAP_CLICKS_TO_CALL | CONTACT | ENABLED primary | contact attempt | primary (bids) | high — overlaps 7,10,14,15 |
| 2 | Smart campaign ad clicks to call | SMART_CAMPAIGN_AD_CLICKS_TO_CALL | CONTACT | ENABLED primary | contact attempt | primary (bids) | high — overlaps ad-call set |
| 3 | Form | GOOGLE_ANALYTICS_4_CUSTOM (GA4 import) | SUBMIT_LEAD_FORM | ENABLED primary | form attempt | primary (bids) | high — generic name; overlaps 9,11,12 |
| 4 | onsiteconcretelandscape.com (web) purchase | GOOGLE_ANALYTICS_4_PURCHASE (GA4 import) | PURCHASE | HIDDEN secondary | n/a (ecommerce legacy) | reporting only | low — keep hidden; remove only after history check |
| 5 | Local actions - Directions | GOOGLE_HOSTED | GET_DIRECTIONS | ENABLED primary | micro | primary (bids) | high — micro as primary |
| 6 | Smart campaign map directions | SMART_CAMPAIGN_MAP_DIRECTIONS | GET_DIRECTIONS | ENABLED primary | micro | primary (bids) | high — micro as primary |
| 7 | Clicks to call | GOOGLE_HOSTED | CONTACT | ENABLED primary | phone click | primary (bids) | high — overlaps 10,14,15 |
| 8 | Calls from Smart Campaign Ads | SMART_CAMPAIGN_TRACKED_CALLS | PHONE_CALL_LEAD | ENABLED primary | ad call | primary (bids) | medium — needs duration/connected definition |
| 9 | Web Contact Form | WEBPAGE | CONTACT | ENABLED primary | form event (gtm.formSubmit) | primary (bids) | high — overlaps 3,11,12; no acceptance proof |
| 10 | Web Phone Calls | WEBPAGE | CONTACT | ENABLED primary | phone click (tel: link) | primary (bids) | high — click ≠ connected call; overlaps 7,14,15 |
| 11 | Submit lead form | WEBPAGE | SUBMIT_LEAD_FORM | ENABLED primary | form event | primary (bids) | high — overlaps 3,9,12 |
| 12 | OnSite — Form Submission | WEBPAGE | SUBMIT_LEAD_FORM | ENABLED primary | form event | primary (bids) | high — overlaps 3,9,11 |
| 13 | OnSite — Phone Call from Ads | AD_CALL | PHONE_CALL_LEAD | ENABLED primary | ad call | primary (bids) | medium — overlaps 2,8; needs connected-call rule |
| 14 | OnSite — Phone Click from Website | CLICK_TO_CALL | PHONE_CALL_LEAD | ENABLED primary | phone click | primary (bids) | high — click ≠ connected; overlaps 7,10,15 |
| 15 | Phone Call Click | WEBPAGE | CONTACT | ENABLED primary | phone click | primary (bids) | high — overlaps 7,10,14 |

Counting method, attribution windows, values, and include-in-conversions flags beyond primaryForGoal were not in the API export — see Data gaps. Sep 8 transcription previously observed Web Phone Calls = One / 30-day click-through / data-driven; do not assume the other 13 match.

## Issues table

| # | Issue | Affected actions | Evidence | Why it matters | Fix | Priority |
|---|---|---|---|---|---|---|
| 1 | Same form counted by 4 primary actions | 3, 9, 11, 12 | 4 SUBMIT_LEAD_FORM/CONTACT form actions all ENABLED primary; GTM Tag 7 fires on generic `gtm.formSubmit` (Sep 5 receipt); 32 form events Apr–Sep never reconciled to accepted delivery | One submission can report as up to 4 conversions; PMax Maximize Conversions learns on inflated volume | Keep exactly one form action primary; set other three secondary; fire only on success-confirmed receipt with idempotency key | P0 |
| 2 | Same phone tap counted by 4+ primary actions | 1, 7, 10, 14, 15 | Parallel CONTACT/CLICK_TO_CALL website-phone actions all primary; GTM Tag 4 fires on `tel:` link click (Sep 5 receipt); Sep 8: 65 phone-click events ≠ connected calls | Clicks bid as leads; Jun–Jul "54 phone calls" are taps, not conversations | Keep exactly one website-phone action (secondary until connected-call tracking exists); demote rest to secondary; track connected/service-qualified stages downstream per measurement design | P0 |
| 3 | Micro conversions primary | 5, 6 Directions | Both GET_DIRECTIONS ENABLED primary | Bidding pays to maximize map taps instead of estimates | Set both Directions to secondary (reporting only) | P0 |
| 4 | Ad-call overlap, no connected definition | 2, 8, 13 | Three parallel ad-call actions all primary; 3 Received rows vs 1 call conversion (Sep 8 §2); no duration/connected threshold on file | Same call may convert twice; 7–10s calls bid equal to 38s+ calls | Keep one ad-call action primary with explicit minimum-duration/connected rule; others secondary; reconcile against phone log | P0 |
| 5 | No qualified-estimate primary | all | Blueprint + measurement design require `qualified_estimate` primary; none exists in export | Account cannot bid toward revenue outcome; stuck optimizing raw contacts | Build offline/CRM `qualified_estimate` import keyed by `conversion_transaction_key` (opportunity key); promote to sole primary only after backfill + QA | P1 (blocks bidding migration) |
| 6 | Generic submission trigger, no dedup key | 9–12, GTM | `gtm.formSubmit` + no transaction/lead ID (Sep 5 readiness; correction packet) | Refreshes, retries, double-submits create new conversions | Success-state trigger + `source_event_key` idempotency; exclude tests via marker | P1 |
| 7 | Naming breaks taxonomy | 3, 9, 11, 12, 15 | "Form" vs "Submit lead form" vs "Web Contact Form" vs "OnSite — Form Submission"; "Phone Call Click" | Operators cannot tell source/stage; future imports will collide | Rename to `[Platform] - [Stage] - [Action] - [Method]`, e.g. `Ads - Lead - Form Submit - Native Tag (success-only)` | P2 |
| 8 | GA4 purchase import noise | 4 | GA4 purchase action present (HIDDEN, secondary) on a lead-gen site | Confusing taxonomy; risk someone flips it primary | Leave HIDDEN; document as legacy; delete only after reporting-history check | P2 |
| 9 | GA4 import Off + no access | 3, 4 | Import app/web metrics Off; Missing permissions on property 477847660 (Sep 8 §5) | Cannot validate GA4↔Ads gap; direction of divergence unknown | Admin restores access; enable import read-only check; compare sessions vs clicks per platform | P1 |
| 10 | Netlify lane invisible to Ads | Sep 10 Search campaign → Netlify page | Form-destination trace: Netlify page loads no GTM/gtag, 0 submissions on live variant; Search campaign never served | Future Search leads bypass all 15 actions; spend with zero attribution | Tag Netlify page (GTM + success-only form + phone-click) or read Netlify Forms directly; do not launch $15/day Search until instrumented | P1 |

## Deduplication review

- Likely duplicates: {3,9,11,12} (one WordPress submit → up to 4 primaries); {7,10,14,15} (one `tel:` tap → up to 4 primaries); {5,6} (one directions tap → 2 primaries); {1,2,8,13} ad-call cluster needs call-ID join to confirm.
- Possible duplicates: GA4 "Form" (#3) vs native webpage forms (#9,11,12) — same submission via tag + import both primary is a duplication finding at any volume.
- Missing keys: no transaction ID / lead ID / `canonical_lead_key` / `conversion_transaction_key` anywhere in evidence. Refresh/retry behavior unverified.
- Recommended primary vs secondary after fix: primary = exactly one `qualified_estimate` (offline import, one per opportunity). Secondary = one success-only form, one connected-call action, one ad-call action, directions, raw clicks. Everything else secondary or removed after history review.
- Proof required (counts, not screenshots): before/after event counts over matched windows excluding lag + transaction-ID spot check, per skill post-fix rule.

## Implementation checklist (approval-gated — do not apply without explicit authority)

- Google Ads: demote to secondary — all but one form, all but one website-phone, both Directions, duplicate ad-calls. Define surviving ad-call minimum duration. Create `qualified_estimate` offline action (secondary until validated). No deletions until reporting/bidding impact reviewed.
- GA4: restore access; audit key events; enable import check; align timezone/attribution definitions.
- GTM (GTM-PFJ633DF): Tag 7 → success-state only + test exclusion + idempotency key; Tag 4 → keep as click telemetry, mark secondary, do not relabel as connected call; Netlify page → add container or document alternate read path.
- CRM/offline: stand up reconciliation table per `contact-reconciliation-template-v1.json`; import `qualified_estimate` once per opportunity via `conversion_transaction_key`.
- Naming: apply `[Platform] - [Funnel Stage] - [Action] - [Source/Method]` to all 15.
- Post-fix QA: run measurement-design QA matrix (valid form, qualified call, repeat, refresh, test, unconnected, unqualified, form+call, two projects, import retry); require Ads = reconciliation = downstream counts for same window.

## Route to next

- After dedup fix with count proof → `bidding-strategy-sanity-check-google-ads` (PMax MAXIMIZE_CONVERSIONS has no trustworthy primary today; do not move to tCPA/tROAS first).
- When CRM loop exists → `offline-conversion-import-audit-google-ads`.

## Data gaps

- Counting method, click-through / engaged-view / view-through windows, attribution model, value settings for 14 of 15 actions (only Web Phone Calls partially known from Sep 8 transcription). Firms up issues 1–4 severity.
- Campaign↔conversion-goal membership (which primaries each campaign bids on). Firms up bidding exposure.
- Change history + recording health + last-recorded dates per action. Firms up legacy vs active split.
- GA4 event list, GTM container export, WordPress form recipient/delivery proof, phone-log/CRM disposition. Firms up qualified-estimate build.
- Netlify + Zapier hook destination check. Firms up Search-lane readiness.

## Notes on this week's numbers

- Sep 9–15: 5 campaigns, 0 clicks, 0 impressions, $0 spend, 0 recorded events. Zero-delivery week — not a conversion-rate signal.
- Jun–Jul PMax "79 conversions / $3.05" = 54 phone-click + 25 form events, `all_conversions` = `conversions` (real recorded events, per Sep 16 plan) — but recorded ≠ qualified. Keep separate from any lead or booking claim.
- Conversion reporting is pending validation for optimization purposes: primary volume exists historically, but its composition (clicks, duplicates, micros) makes it unsuitable for bid decisions until issues 1–5 are closed.

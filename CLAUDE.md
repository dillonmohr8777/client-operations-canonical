@AGENTS.md

# Client Operations

Everything lives in `AGENTS.md`. Read it.

Merged 2026-09-17. This file used to carry its own orientation section — the
registry as source of truth, which of the seven copies is real, the four known
defects, the hard boundaries. Muse ignores `CLAUDE.md` wherever an `AGENTS.md`
exists in the same directory, so none of that reached a Muse run. It now lives
in `AGENTS.md`, which every harness reads, and this file imports it above.

Do not add rules here. Add them to `AGENTS.md`, or they will be invisible to
Muse and to anything else that follows the `AGENTS.md` convention.

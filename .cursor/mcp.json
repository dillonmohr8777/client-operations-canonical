{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote@latest",
        "https://connect.composio.dev/mcp",
        "--header",
        "x-consumer-api-key:${COMPOSIO_KEY}",
        "--silent"
      ]
    }
  }
}
